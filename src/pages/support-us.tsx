import type { NextPage } from 'next'
import Layout from '../components/layout';
import SupportUsContainer from '../sections/support-us';
const SupportUs: NextPage = () => {
    return (
        <Layout>
            <SupportUsContainer />
        </Layout>
    )
}

export default SupportUs
